{
    "builds": [{
      "src": "Start/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "1000mb", "runtime": "python3.11.3" }
    }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "Start/wsgi.py"
      }
    ]
}